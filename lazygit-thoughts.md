It would be great to get the program running so that it can all essential tasks
via cli instead of just tui. Then it would be great to implement a plugin for
lazygit, which could be upstreamed when this program gets to the point where
roll-flow could work for others.
